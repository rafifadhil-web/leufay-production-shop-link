import { BrandLogo } from '@/components/BrandLogo'; import { ProductBrowser } from '@/components/ProductBrowser'; import { getCategories,getPublishedProducts } from '@/lib/products';
export default async function Home(){const [products,categories]=await Promise.all([getPublishedProducts(),getCategories()]); return <><header className="shell" style={{padding:'19px 0 8px',display:'flex',alignItems:'center',justifyContent:'space-between'}}><span className="eyebrow">LEUFAY PRODUCTION</span><a className="meta" href="#collection">SHOP ALL ↓</a></header><main><section className="hero"><div className="shell"><BrandLogo/><p className="eyebrow">LEUFAY LINK SHOPPE</p><h1>DISCOVER YOUR<br/>NEXT FAVORITE.</h1><p>Curated finds, fashion, collectibles, merchandise &amp; more.</p><a href="#collection" className="button">EXPLORE COLLECTION</a></div></section><ProductBrowser products={products} categories={categories}/></main><footer className="footer"><div className="shell">LEUFAY LINK SHOPPE <span style={{color:'var(--accent)'}}>✦</span> by LEUFAY PRODUCTION</div></footer></>;}
