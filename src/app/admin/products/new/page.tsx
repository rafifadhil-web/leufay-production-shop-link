import { ProductForm } from '@/components/ProductForm'; import { getCategories } from '@/lib/products'; import { saveProduct } from '../../actions'; import { AdminProtected } from '@/components/AdminProtected'; export default async function NewProduct(){const categories=await getCategories();return <AdminProtected><><p className="eyebrow">CATALOG</p><h1 className="section-title">NEW PRODUCT</h1><ProductForm categories={categories} action={saveProduct}/></></AdminProtected>}
