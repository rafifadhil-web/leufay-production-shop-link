import { requireAdmin } from '@/lib/auth'; import { archiveCategory,saveCategory } from '../actions'; import { AdminProtected } from '@/components/AdminProtected';
export default async function Categories(){const {supabase}=await requireAdmin();const {data: categoryRows}=await supabase.from('categories').select('id,name,slug,description,archived').order('name');const data=categoryRows??[];return <AdminProtected><><p className="eyebrow">CATALOG</p><h1 className="section-title">CATEGORIES</h1><form action={saveCategory} className="card form" style={{padding:18,marginBottom:20,maxWidth:560}}><div className="field"><label htmlFor="name">CATEGORY NAME</label><input id="name" name="name" required placeholder="e.g. Watches"/></div><div className="field"><label htmlFor="description">DESCRIPTION</label><input id="description" name="description"/></div><button className="button" type="submit">ADD CATEGORY</button></form><div className="card table-wrap"><table className="data-table"><thead><tr><th>NAME</th><th>SLUG</th><th>STATUS</th><th>ACTION</th></tr></thead><tbody>{data.map(c=><tr key={c.id}><td>{c.name}</td><td className="meta">{c.slug}</td><td>{c.archived?'ARCHIVED':'ACTIVE'}</td><td>{!c.archived&&<form action={archiveCategory.bind(null,c.id)}><button className="meta" style={{border:0,background:'none',color:'#ffab65',padding:0,cursor:'pointer'}}>ARCHIVE</button></form>}</td></tr>)}</tbody></table></div></></AdminProtected>}
