'use client'; import { useState } from 'react'; import { createClient } from '@/lib/supabase/client'; import { useRouter } from 'next/navigation';
const LOGIN_TIMEOUT_MS=15000;
function withTimeout<T>(promise:Promise<T>){let timer:number;const timeout=new Promise<never>((_,reject)=>{timer=window.setTimeout(()=>reject(new Error('Login timed out. Please check your connection and try again.')),LOGIN_TIMEOUT_MS)});return Promise.race([promise,timeout]).finally(()=>window.clearTimeout(timer!))}
export default function Login(){const router=useRouter(),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const f=new FormData(e.currentTarget);try{const supabase=createClient();const {error:loginError}=await withTimeout(supabase.auth.signInWithPassword({email:String(f.get('email')),password:String(f.get('password'))}));if(loginError){setError(loginError.message);return}router.replace('/admin');router.refresh()}catch(cause){setError(cause instanceof Error?cause.message:'Login failed. Please try again.')}finally{setLoading(false)}}return <main className="shell" style={{padding:'80px 0'}}><div className="card" style={{padding:24,maxWidth:440,margin:'auto'}}><p className="eyebrow">LEUFAY PRODUCTION</p><h1 className="section-title">ADMIN LOGIN</h1><form className="form" onSubmit={submit}><div className="field"><label htmlFor="email">EMAIL</label><input id="email" name="email" type="email" required autoComplete="email"/></div><div className="field"><label htmlFor="password">PASSWORD</label><input id="password" name="password" type="password" required autoComplete="current-password"/></div>{error&&<p role="alert" style={{margin:0,color:'#ffab65',fontSize:'.85rem'}}>{error}</p>}<button className="button" disabled={loading}>{loading?'LOGGING IN…':'LOGIN'}</button></form></div></main>}
