import Link from 'next/link'; import type { Product } from '@/types';
const cta = (platform: string) => platform === 'shopee' ? 'BELI DI SHOPEE' : 'BELI SEKARANG';
export function ProductCard({ product }: { product: Product }) { return <article className="card product-card"><Link href={`/product/${product.slug}`} aria-label={`Lihat ${product.name}`}><div className="product-art">{product.image_url ? <img src={product.image_url} loading="lazy" alt={product.name} /> : <span className="placeholder">PRODUCT IMAGE<br/>COMING SOON</span>}{product.featured && <span className="badge">FEATURED</span>}</div></Link><div className="product-copy"><div className="meta">{product.brand} · {product.category?.name.toUpperCase()}</div><h3><Link href={`/product/${product.slug}`}>{product.name}</Link></h3><p>{product.description}</p><a className="button orange" href={product.affiliate_url} target="_blank" rel="noopener noreferrer">{cta(product.affiliate_platform)}</a></div></article>; }
