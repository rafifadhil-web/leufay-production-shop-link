import type { Category,Product } from '@/types';
export function ProductForm({categories,product,action}:{categories:Category[];product?:Product;action:(data:FormData)=>void|Promise<void>}){return <form className="form card" style={{padding:20}} action={action}><div className="field"><label htmlFor="name">PRODUCT NAME *</label><input id="name" name="name" defaultValue={product?.name} required/></div><div className="field"><label htmlFor="brand">BRAND *</label><input id="brand" name="brand" defaultValue={product?.brand} required/></div><div className="field"><label htmlFor="category_id">CATEGORY *</label><select id="category_id" name="category_id" defaultValue={product?.category_id} required><option value="">Select category</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></div><div className="field"><label htmlFor="description">DESCRIPTION</label><textarea id="description" name="description" defaultValue={product?.description??''}/></div><div className="field"><label htmlFor="image">PRODUCT IMAGE (JPG, PNG, WEBP • MAX 5 MB)</label><input id="image" name="image" type="file" accept="image/jpeg,image/png,image/webp"/></div><div className="field"><label htmlFor="affiliate_platform">AFFILIATE PLATFORM</label><select id="affiliate_platform" name="affiliate_platform" defaultValue={product?.affiliate_platform??'shopee'}>{['shopee','tokopedia','lazada','website'].map(x=><option key={x} value={x}>{x.toUpperCase()}</option>)}</select></div><div className="field"><label htmlFor="affiliate_url">AFFILIATE URL *</label><input id="affiliate_url" name="affiliate_url" type="url" defaultValue={product?.affiliate_url} required/></div><div className="field"><label htmlFor="status">STATUS</label><select id="status" name="status" defaultValue={product?.status??'draft'}>{['draft','published','archived'].map(x=><option key={x} value={x}>{x.toUpperCase()}</option>)}</select></div><label style={{display:'flex',gap:9,alignItems:'center',fontSize:'.85rem'}}><input name="featured" type="checkbox" defaultChecked={product?.featured}/> FEATURED PICK</label><button className="button" type="submit">SAVE PRODUCT</button></form>}
